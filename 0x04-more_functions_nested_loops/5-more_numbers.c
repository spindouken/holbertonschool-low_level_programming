#include "main.h"
/**
 * more_numbers - we're going to multiply
 *
 * Return: void (infinite possible returns)
 */
void more_numbers(void)
{
	int x;
	int y;

	for (x = 0; x <= 14; x++)
	{
		for (y = 0; y <= 10; y++)
		(return = x * y)
	}
	_putchar(return + 48);
	_putchar('\n');
}
