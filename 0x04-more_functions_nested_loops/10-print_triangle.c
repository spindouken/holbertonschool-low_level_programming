#include "main.h"
/**
 * print_triangle - generate triangle extendable through variable
 *
 * @size: variable
 *
 * Return: void
 */
void print_triangle(int size)
{
	int x;

	if (size <= 0)
	{
		_putchar('\n');
	}
	for (n <= 0)
	{
		_putchar('\n');
	}
	for (x = 0; x < n; x++)
	{
		_putchar(92);
		_putchar('\n');
	}
}
