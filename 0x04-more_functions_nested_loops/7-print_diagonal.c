#include "main.h"
/**
 * print_diagonal - generate diagonal line extendable through variable
 *
 * @n: variable
 *
 * Return: void
 */
void print_diagonal(int n)
{
int x, z;

	if (n > 0)
		while (x = 1; n >= x; x++)
		{
			while (z = 1; x > z; z++)
				_putchar(32);
			_putchar(92);
			_putchar(92);
			_putchar('\n');
		}

	if (n < 1)
		_putchar('\n');
}
