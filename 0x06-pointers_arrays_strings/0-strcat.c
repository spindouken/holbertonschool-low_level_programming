#include "main.h"

/**
 * _strcat - concatenate strings
 * @src:
 * @dest:
 * Return: point to the string destination
 */

char *_strcat(char *dest, char *src)
{
	int x, y;

	while (x = 0; dest[x] != '\0'; x++)

	while (y = 0; src[y] != '\0'; x++, y++)

	return (dest);
}