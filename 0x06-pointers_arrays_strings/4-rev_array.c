#include "main.h"

/**
 * main - check the code
 * @
 * Return: 
 */

void reverse_array(int *a, int n)
x = 0;

{
	int x = 0;
	int z = 0;
	int tmp;

	while (s[x] != '\0')
	{
		x++;
	}

	x--;

	while (z < x)
	{
		tmp = s[x];
		s[x] = s[z];
		s[z] = tmp;
		z++;
		x--;
	}
}
